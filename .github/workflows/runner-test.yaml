name: Runner Test

on:
  workflow_dispatch:

jobs:
  test-runner:
    runs-on: self-hosted

    steps:
      - name: Check runner
        run: |
          echo "=== Runner ==="
          hostname
          whoami

          echo "=== Docker ==="
          docker --version
          docker compose version

          echo "=== Docker access ==="
          docker ps